# Template Patch Draft - v1.8.4

## Scope
This file collects implemented and planned items for the v1.8.4 template patch. It is not a changelog.
Only the Implemented section may be merged into .template/docs/changelog.md and only after the patch is released.

## Status
Draft. Contains planned items.

## Implemented (ready for changelog)
- Bootstrap: allow explicit target repo via `-TargetRepoPath`.
- Bootstrap: when run from template source, target the current working directory unless it is inside the template source (then require `-TargetRepoPath`).

## Planned (draft, not implemented)
- None recorded yet. Add planned items below and keep them clearly marked as planned.

## Merge notes
- When v1.8.4 is released, move only implemented items into .template/docs/changelog.md under v1.8.4.
- Do not move planned items until implemented.
